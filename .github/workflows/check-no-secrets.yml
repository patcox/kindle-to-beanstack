name: check-no-secrets
on:
  push:
  pull_request:
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Scan tracked files for real Amazon account identifiers
        run: bash scripts/check-no-secrets.sh
